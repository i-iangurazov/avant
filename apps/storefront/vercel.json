{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "printf '\\nERROR: apps/storefront cannot be deployed with bare `vercel --prod` from this folder.\\nRun `pnpm deploy` from apps/storefront, or run `cd ../.. && vercel --local-config vercel.storefront.json --prod`.\\n\\n' && exit 1"
}
